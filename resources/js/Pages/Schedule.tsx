import Nav from "@/Components/Nav";
import React from "react";

export default function Schedule(props: any): React.JSX.Element {
    return (
        <>
            <Nav auth={props.auth}></Nav>
        </>
    );
}
